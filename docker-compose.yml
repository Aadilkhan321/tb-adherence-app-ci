version: '3.8'

services:
  # TB Adherence App
  app:
    build: .
    container_name: tb-adherence-app
    ports:
      - "3000:3000"  # Client port
      - "5000:5000"  # Server port
    environment:
      - NODE_ENV=development
      - PORT=5000
    volumes:
      - ./server:/app/server
      - ./client/src:/app/client/src
      - /app/node_modules
      - /app/server/node_modules
      - /app/client/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Optional: Database for production use
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: tb-adherence-db
  #   environment:
  #     POSTGRES_DB: tb_adherence
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

  # Optional: Redis for session storage
  # redis:
  #   image: redis:7-alpine
  #   container_name: tb-adherence-redis
  #   ports:
  #     - "6379:6379"
  #   restart: unless-stopped

# volumes:
#   postgres_data: